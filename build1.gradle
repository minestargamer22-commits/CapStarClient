plugins {
    id 'java'
}

group = 'com.capstar.client'
version = '1.1.0'

repositories {
    mavenCentral()
    maven { url "https://files.minecraftforge.net/maven" }
}

dependencies {
    // اینجا معمولاً Forge MDK وابستگی‌ها را فراهم می‌کند؛ برای Actions از MDK استفاده خواهد شد
}

jar {
    manifest {
        attributes(
            'Main-Class': 'com.capstar.client.CapStarMod'
        )
    }
}
